version: '3.8'
services:
  db:
    image: postgres:15-alpine
    restart: always
    environment:
      POSTGRES_USER: walaaplus
      POSTGRES_PASSWORD: walaaplus_password
      POSTGRES_DB: loyalty
    ports:
      - '5433:5432'
